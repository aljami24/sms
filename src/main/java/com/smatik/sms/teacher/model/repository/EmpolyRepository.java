package com.smatik.sms.teacher.model.repository;

import com.smatik.sms.teacher.model.entity.Employ;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmpolyRepository extends JpaRepository<Employ, Long> {
}
